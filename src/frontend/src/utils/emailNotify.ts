// Email notifications removed — replaced with in-app notifications via notificationStore
export function emailNotify(_subject: string, _body: string) {
  // no-op: use useNotificationStore.addNotification instead
}
